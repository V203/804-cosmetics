let BtnPM = (props: any) => {

    return (
        <>
            <input className="btn glow" onClick={()=> ""} type={"button"} style={{width:props.width, height:props.height}} value ={props.sign}/>
                
                    
                
        </>
    )

}

export default BtnPM;